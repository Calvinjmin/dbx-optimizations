venv/
.vscode/*
.claude/
.vibe-demo.yaml
.DS_Store

# Local bundle config — copied from databricks.example.yml and edited with
# the user's workspace host + warehouse id. Not committed.
databricks.yml
